---
title: "Login Subscriber"
api: "POST /v1/public/subscriber.login"
description: "Authenticate a subscriber with email and password. Returns JWT token for subsequent requests."
---

## Authentication

This endpoint requires API key authentication via the `Authorization` header.

```bash
Authorization: Bearer YOUR_API_KEY
```

## Request Body

<ParamField body="email" type="string" required>
  Subscriber's email address
</ParamField>

<ParamField body="password" type="string" required>
  Subscriber's password
</ParamField>

## Response

<ResponseField name="data" type="object">
  Subscriber object
</ResponseField>

<ResponseField name="code" type="string">
  JWT token for authentication
</ResponseField>

<RequestExample>

```bash cURL
curl --location --request POST 'https://api.userorbit.com/v1/public/subscriber.login' \
--header 'Authorization: Bearer YOUR_API_KEY' \
--header 'Content-Type: application/json' \
--data-raw '{
  "email": "jane@example.com",
  "password": "SecurePass123!"
}'
```

```javascript Node.js
const response = await fetch('https://api.userorbit.com/v1/public/subscriber.login', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    email: 'jane@example.com',
    password: 'SecurePass123!'
  })
});

const data = await response.json();
```

```python Python
import requests

response = requests.post(
    'https://api.userorbit.com/v1/public/subscriber.login',
    headers={
        'Authorization': 'Bearer YOUR_API_KEY',
        'Content-Type': 'application/json'
    },
    json={
        'email': 'jane@example.com',
        'password': 'SecurePass123!'
    }
)

data = response.json()
```

</RequestExample>

<ResponseExample>

```json Response
{
  "data": {
    "id": "87654321-1234-1234-1234-123456789012",
    "name": "Jane Smith",
    "email": "jane@example.com",
    "source": "api",
    "createdAt": "2024-01-15T10:30:00Z"
  },
  "code": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

</ResponseExample>

## Error Responses

```json
{
  "error": "Invalid credentials",
  "statusCode": 401
}
```

## Notes

- Returns 401 if email or password is incorrect
- JWT token expires after 30 days
- Email matching is case-insensitive
